import AppFooterStyle from "./AppFooter.style";
import { makeStyles } from "@material-ui/core";

const useAppFooterStyle = makeStyles(AppFooterStyle, { name: "AppFooter" });

export { useAppFooterStyle };

export { default } from "./AppFooter.style";
