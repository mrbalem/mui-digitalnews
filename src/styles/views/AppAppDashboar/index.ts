import { makeStyles } from "@material-ui/core";
import AppDahsStyle from "./AppDash.styles";

const useAppDashStyle = makeStyles(AppDahsStyle, { name: "ProductCTA" });

export { useAppDashStyle };
export { default } from "./AppDash.styles";
